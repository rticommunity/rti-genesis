# Minimal build-system config to enable PEP 517 builds.
# We keep metadata in setup.py for now and will migrate
# to PEP 621 ([project]) in a later change.

[build-system]
requires = [
  "setuptools>=61",
  "wheel",
]
build-backend = "setuptools.build_meta"

